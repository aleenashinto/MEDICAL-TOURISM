import { HospitalPortalView } from "@/components/HospitalPortalView";

export const metadata = {
  title: "Hospital Partner Portal | Vitalis Global",
  description: "Direct hospital case reviews, quotation uploads, and admission tracking.",
};

export default function HospitalPage() {
  return <HospitalPortalView />;
}
